# Debug Interface Triage

1. Find silkscreen labels: TX RX GND VCC TDI TDO TCK TMS  
2. Match voltage levels before connecting  
3. Read-only serial logs first  
4. Note the U-Boot interrupt key and env vars (do not casually saveenv)  
5. SHA256 the extracted image  

