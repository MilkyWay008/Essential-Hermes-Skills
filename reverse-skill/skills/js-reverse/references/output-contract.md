# Output Contract

The final output should at least include:

- The target request and parameter location
- Which script and which function participate in generating it
- Evidence sources: requests, call stacks, breakpoints, runtime values
- Whether it can currently be reproduced stably
- If incomplete, which single environment gap remains

